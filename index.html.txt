<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cash App $650 Gift Card</title>
  <style>
    /* Reset */
    body, html {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      background: linear-gradient(120deg, #4caf50, #81c784);
      color: #fff;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      overflow: hidden;
    }

    /* Container */
    .container {
      width: 350px;
      background: #fff;
      color: #333;
      border-radius: 10px;
      box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
      overflow: hidden;
      text-align: center;
      animation: bounce 1.5s infinite alternate;
    }

    /* Header Menu */
    .menu-btn {
      position: absolute;
      top: 10px;
      left: 10px;
      font-size: 20px;
      cursor: pointer;
      background: none;
      border: none;
      color: #fff;
    }

    .menu-btn a {
      text-decoration: none;
      color: inherit;
    }

    /* Gift Image */
    .gift-image {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    /* Content */
    .content {
      padding: 20px;
    }

    h1 {
      font-size: 22px;
      color: #4caf50;
      margin-bottom: 10px;
    }

    p {
      font-size: 14px;
      margin-bottom: 20px;
    }

    /* Claim Button */
    .claim-btn {
      display: inline-block;
      padding: 10px 20px;
      font-size: 16px;
      background: #4caf50;
      color: #fff;
      border: none;
      border-radius: 5px;
      text-decoration: none;
      cursor: pointer;
      animation: pulse 1.5s infinite;
    }

    .claim-btn:hover {
      background: #388e3c;
    }

    /* Offer Details */
    .offer-details {
      padding: 15px;
      font-size: 14px;
      background: #f8f8f8;
      border-top: 1px solid #ddd;
    }

    /* Pop-up Notification */
    .popup {
      position: absolute;
      bottom: 10px;
      left: 50%;
      transform: translateX(-50%);
      background: rgba(0, 0, 0, 0.8);
      color: #fff;
      padding: 10px 20px;
      border-radius: 20px;
      font-size: 14px;
      display: none;
      animation: fadein 1s ease-in-out;
    }

    /* CSS Animations */
    @keyframes bounce {
      0% { transform: translateY(0); }
      100% { transform: translateY(-10px); }
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.1); }
      100% { transform: scale(1); }
    }

    @keyframes fadein {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }

    /* Responsive Design */
    @media (max-width: 480px) {
      .container {
        width: 90%;
      }
    }
  </style>
</head>
<body>

  <!-- Header -->
  <button class="menu-btn"><a href="https://smrturl.co/a/s580b9e7f2a/3956?s1=me">☰</a></button>

  <!-- Landing Page Content -->
  <div class="container">
    <img src="https://i.ibb.co/bWf4XZz/8ecb6879cb13.jpg" alt="Cash App Gift Card" class="gift-image">
    <div class="content">
      <h1>Claim Your $650 Cash App Gift Card!</h1>
      <p>Register now and get your exclusive gift card. Limited time offer!</p>
      <a href="https://smrturl.co/a/s580b9e7f2a/3956?s1=me" class="claim-btn">Claim Now</a>
    </div>
    <div class="offer-details">
      <p>💬 4578 people have already claimed this offer!</p>
      <p>🎁 Safe and 100% guaranteed gift card.</p>
    </div>
  </div>

  <!-- Pop-up Notification -->
  <div class="popup" id="popup">4578 people have already claimed this!</div>

  <!-- JavaScript -->
  <script>
    // Pop-up Notification
    window.onload = () => {
      const popup = document.getElementById('popup');
      setTimeout(() => { popup.style.display = 'block'; }, 3000);
    };
  </script>
</body>
</html>
